type ThemeType = {
  headerFontSize: string;
  paragraphFontSize: string;
  colors: {
    black: string;
    darkRed: string;
    white: string;
    blue: string;
    darkGrey: string;
  };
};

export default ThemeType;